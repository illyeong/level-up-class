# 문의 3건 처리 내역 (2026-09-03)

코드 수정 완료. 운영 배포·Firebase 요금제 변경·운영 데이터 수정은 수행하지 않음.

## 1. 학급 퀘스트 / 우리반 대작전 서버 한도

- 퀘스트 관리·상세·키오스크·학생 퀘스트·대시보드의 전체 학생/퀘스트 조회를 교사 범위 서버 쿼리 + 학급 필터로 변경.
- 종료된 퀘스트는 목록에서 완료 기록을 다시 읽지 않음.
- 관리 목록 조회 실패를 '0명 완료'로 숨기지 않고 오류와 다시 불러오기 표시.
- 상세/키오스크의 반복 초기화는 KST 일일/주간 기준으로 화면에서 판정. 상세를 열기만 해도 과거 completion을 삭제하던 쓰기 제거.
- 대작전 학생 화면은 오늘 공격만 기본 구독. 전체 기록은 '전체 누적 기록 보기'로 열 수 있음. HP와 하루 1회 공격 트랜잭션 유지.
- 서버 한도/브라우저 저장공간/네트워크 오류 구분 및 재연결 경로 제공.
- **프로젝트 무료 한도는 그대로임.** 다른 화면의 읽기까지 포함해 초과하면 제한될 수 있으며, 실제 재발률은 운영 배포 후 사용량으로 확인해야 함.

## 2. 주제글쓰기 보상 없이 확인 / 삭제

- 주제글쓰기 관리 → 제출물 → '보상 없이 확인'. 지급 대기·대시보드 배지·일괄 보상 대상에서 제외.
- '글 삭제'는 복구 가능한 삭제. 학생 목록에서 숨기고 '삭제된 글'에서 복구. 이미 지급한 보상과 보상 로그는 유지.
- 확인만 한 글도 교사가 명시적으로 '보상 지급'을 선택하면 나중에 지급 가능.
- 지급·확인·삭제 시 최신 제출물 상태를 트랜잭션에서 검사. 오래된 일괄 승인 목록과 동시 지급으로 중복 보상하지 않음.
- 진행 중인 학생 AI 채점 응답이 교사의 확인/보상 상태를 덮어쓰지 않도록 보호.

## 3. 교사의 AI 학습 콘텐츠 지정

- AI 학습 관리 → '학습 콘텐츠 지정' 탭.
- 전체 허용 / 선택한 학년만 허용 / 지정한 단원·차시만 허용.
- 학년을 전환하며 여러 차시 선택 가능. 단원 전체 선택과 선택 목록 확인·해제 지원.
- '학급에 적용'을 눌러 저장. 기존 학습 기록과 교사 미리보기는 유지.
- 학생 목록·차시 목록·미리 로딩을 제한하며, 학습 진입/재시작/보상 저장 전 최신 서버 설정 확인.
- 새 설정 경로: `aiCoursewareAccess/class_{classId}`. 학급 ID가 없는 기존 교사 단위 화면은 `aiCoursewareAccess/teacher_{teacherUid}` 사용. 새 문서가 없는 학급은 기존 전체 허용 동작 유지.
- 설정 조회 오류/오프라인 캐시만 있을 때는 전체 허용으로 풀지 않음.
- 현재 앱의 클라이언트 학습 경로에 적용되는 제한이며, 악의적인 직접 DB/API 호출을 통제하는 서버 보안 규칙 추가는 이번 범위에 포함하지 않음.

## 검증

- `npm run test:support`: 가상 Firestore, 운영 접속 없음. 무보상 확인, 삭제·복구, 기존 보상 유지, 오래된 일괄 승인 목록, 동시 중복 지급, 다른 교사/학급 차단, 허용 학년/차시, KST 일일/주간 경계 통과.
- `npm run test:class-operation`: 서버/브라우저 한도 구분 및 효과 실패 검사 통과.
- `npm run test:courseware`: 기존 수학/선택지/그림/API QA와 반영 안전 검사 통과.
- `npm run build`: 통과. 기존 번들 크기 경고 남음.
- 새 유틸리티·설정 컴포넌트·훅·글쓰기 화면 및 대작전 학생 화면의 대상 ESLint 검사 통과. 기존 큰 퀘스트/학습 화면 전체에는 기존 lint 문제와 상태 동기화 effect 진단이 남음.
- 가상 브라우저: 특정 5학년 차시만 지정했을 때 학생에게 5학년과 해당 차시만 표시되는 것을 확인. 무보상 확인 후 확인 대기 0건·골드 0 유지 확인.
- 삭제/복구의 트랜잭션 검증은 통과했지만 브라우저 삭제 확인창에서 자동화 연결이 멈춰 UI의 삭제·복구 완료 동작은 끝까지 확인하지 못함.

## 후속

운영 반영 전 새 설정 문서의 실제 Firebase 접근 권한을 확인하고 배포한 뒤, 교사 테스트 계정에서 설정 저장 → 학생 학습관 노출을 확인할 것. 현재 실제 학급 설정을 임의로 바꾸거나 학생 글을 삭제하지 않았음.
